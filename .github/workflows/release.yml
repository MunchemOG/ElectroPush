name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build and Release
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.25'
      
      - name: Get version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
      
      - name: Build binaries
        run: |
          mkdir -p dist
          
          # macOS Intel
          GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w" -o dist/epsh-darwin-amd64
          
          # macOS Apple Silicon
          GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w" -o dist/epsh-darwin-arm64
          
          # Linux
          GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o dist/epsh-linux-amd64
          
          # Windows
          GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o dist/epsh-windows-amd64.exe
          
          # Universal macOS binary
          lipo -create -output dist/epsh-darwin-universal \
            dist/epsh-darwin-amd64 \
            dist/epsh-darwin-arm64
      
      - name: Calculate SHA256
        id: sha
        run: |
          echo "amd64=$(shasum -a 256 dist/epsh-darwin-amd64 | cut -d' ' -f1)" >> $GITHUB_OUTPUT
          echo "arm64=$(shasum -a 256 dist/epsh-darwin-arm64 | cut -d' ' -f1)" >> $GITHUB_OUTPUT
          echo "linux=$(shasum -a 256 dist/epsh-linux-amd64 | cut -d' ' -f1)" >> $GITHUB_OUTPUT
          echo "windows=$(shasum -a 256 dist/epsh-windows-amd64.exe | cut -d' ' -f1)" >> $GITHUB_OUTPUT
      
      - name: Create checksums file
        run: |
          cd dist
          shasum -a 256 epsh-* > SHA256SUMS
          cat SHA256SUMS
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/epsh-darwin-amd64
            dist/epsh-darwin-arm64
            dist/epsh-darwin-universal
            dist/epsh-linux-amd64
            dist/epsh-windows-amd64.exe
            dist/SHA256SUMS
          body: |
            ## Installation
            
            ### macOS (Homebrew)
            ```bash
            brew tap MunchemOG/homebrew-epsh
            brew install epsh
            ```
            
            ### Manual Installation
            
            Download the appropriate binary for your system:
            
            - **macOS (Universal)**: `epsh-darwin-universal`
            - **macOS (Intel)**: `epsh-darwin-amd64`
            - **macOS (Apple Silicon)**: `epsh-darwin-arm64`
            - **Linux**: `epsh-linux-amd64`
            - **Windows**: `epsh-windows-amd64.exe`
            
            Make it executable and move to your PATH:
            ```bash
            chmod +x epsh-darwin-*
            sudo mv epsh-darwin-* /usr/local/bin/epsh
            ```
            
            ### SHA256 Checksums
            
            macOS (Intel): `${{ steps.sha.outputs.amd64 }}`
            macOS (Apple Silicon): `${{ steps.sha.outputs.arm64 }}`
            Linux: `${{ steps.sha.outputs.linux }}`
            Windows: `${{ steps.sha.outputs.windows }}`
            
            ## What's New
            
            See [README.md](https://github.com/MunchemOG/ElectroPush#readme) for full documentation.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
